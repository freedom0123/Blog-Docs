

判断字符串是否符合某种格式

```java
public static boolean isTargetLegal(String pattern, String target) {
    Pattern key = Pattern.compile(pattern);
    if (key.matcher(target).find()) {
        return true;
    }
    return false;
}
```

